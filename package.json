{
  "name": "rest-chat",
  "version": "1.0.0",
  "description": "* start from the up-to-date master branch (`git checkout master; git pull origin master`)\r * Create a feature branch named 'chat-spa' (`git checkout -b chat-spa`)\r * modify the files in this directory to have the require features\r * add, commit, and push the branch to github\r * Create a PR to merge to master\r * Be sure to include the TA(s) and I as reviewers.  \r * Due by 11:59pm (PT) Sun Mar 22",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx webpack"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cookie-parser": "^1.4.5",
    "express": "^4.17.1",
    "uuid": "^7.0.2"
  },
  "devDependencies": {
    "@babel/core": "^7.8.7",
    "@babel/preset-env": "^7.8.7",
    "babel-loader": "^8.0.6",
    "webpack": "^4.42.0",
    "webpack-cli": "^3.3.11"
  }
}
